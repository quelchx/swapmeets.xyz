{
  "scripts": {
    "start": "cd server && npm start",
    "kill": "kill-port 5000 && kill-port 6379",
    "redis-stop": "sudo service redis-server stop",
    "stop": "npm run redis-stop && npm run kill"
  },
  "author": "Eric Quelch",
  "license": "MIT",
  "devDependencies": {
    "@types/react-google-recaptcha": "^2.1.5"
  }
}
